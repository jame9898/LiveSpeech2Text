# ============================================================
# 在线实时语音识别系统 v1.0 — Python 依赖（GPU 版）
# ============================================================
# 适用于 NVIDIA 显卡 + CUDA 12.4 环境
# Python 3.10 ~ 3.12 (推荐 3.12)
#
# 安装方式:
#   pip install -r requirements-gpu.txt
#
# ============================================================

--extra-index-url https://download.pytorch.org/whl/cu124

# --- 深度学习引擎（GPU 版，CUDA 12.4）---
torch>=2.6.0
torchaudio>=2.6.0

# --- 模型下载与管理 ---
modelscope>=1.20.0

# --- Qwen3-ASR 识别引擎 ---
qwen-asr>=0.0.6

# --- GUI 框架 ---
PySide6>=6.5.0

# --- 实时通信 ---
websockets>=12.0

# --- 音频处理 ---
soundfile>=0.12.0

# --- 拼音转换（纠正词匹配用）---
pypinyin>=0.50.0

# --- 科学计算（语音处理基础）---
numpy>=1.24.0

# ============================================================
# 以下为自动依赖（pip 会自动安装，通常无需手动指定）
# ============================================================
# transformers  (qwen-asr 依赖)
# accelerate    (qwen-asr 依赖)

# ============================================================
# 可选依赖
# ============================================================
# funasr>=1.3.0        — Paraformer / SenseVoice / VAD 模型
# scikit-learn>=1.3.0  — 关键词扩展功能
#
# pip install funasr scikit-learn